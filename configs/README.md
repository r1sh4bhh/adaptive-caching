# Configuration

`default.yaml` will hold the default configuration, while `experiments/` will contain ablation configurations. Ablations are config permutations, never code branches.

Planned feature flags:

- `adaptive`
- `tuning`
- `sizeAware`
- `tiers`
- `shadow`
